---
import BaseLayout from "./BaseLayout.astro";
const { frontmatter } = Astro.props;
---
<BaseLayout pageTitle={frontmatter.title}>
  <p>Written by {frontmatter.author}<br />on {frontmatter.pubDate.toString().slice(0,10)}</p>
  <slot />
</BaseLayout>